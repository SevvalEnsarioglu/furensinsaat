// Convenience re-export.
// Import useTheme from this hook rather than directly from context
// to keep component imports clean and context internals encapsulated.
export { useTheme, type Theme } from '../context/ThemeContext';
