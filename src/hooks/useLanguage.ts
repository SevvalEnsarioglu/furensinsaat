// Convenience re-export.
// Import useLanguage from this hook rather than directly from context
// to keep component imports clean and context internals encapsulated.
export { useLanguage, type Lang } from '../context/LanguageContext';
